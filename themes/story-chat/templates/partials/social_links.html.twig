{% for social in site.social_links %}
    <a href="{{ social.link }}" target="_blank">
        <img src="{{ url('theme://images/social/' ~ social.image) }}" alt="{{ social.title }}">
    </a>
{% endfor %}